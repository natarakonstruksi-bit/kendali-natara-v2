{
  "name": "kendali-natara-full-ui-cloudflare",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler d1 migrations apply DB --remote && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}